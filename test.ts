// Test file created and edited
function greet(name: string): string {
	return `Hello, ${name}!`;
}

function farewell(name: string): string {
	return `Goodbye, ${name}!`;
}

console.log(greet("World"));
console.log(farewell("World"));
